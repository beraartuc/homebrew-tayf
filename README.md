# homebrew-tayf

Homebrew tap for [tayf](https://github.com/beraartuc/tayf) — a PTY-based,
terminal-agnostic, regex-driven output colorizer.

## Install

```bash
brew install beraartuc/tayf/tayf
```

Or tap first, then install:

```bash
brew tap beraartuc/tayf
brew install tayf
```
